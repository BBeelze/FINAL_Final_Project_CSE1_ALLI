# Motorcycle CRUD REST API

A secure Flask API to manage a motorcycle inventory with MySQL, JWT auth, and XML/JSON support.

## 🔧 Installation

1. **Database**:  
   Run `motorcycles.sql` to create `motorcycles_db` with sample data.

2. **Python Setup**:
   ```bash
   git clone <your-repo>
   cd motorcycle-api
   python -m venv venv
   source venv/bin/activate  # Linux/macOS
   venv\Scripts\activate     # Windows
   pip install -r requirements.txt